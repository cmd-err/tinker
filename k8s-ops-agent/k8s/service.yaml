apiVersion: v1
kind: Service
metadata:
  name: k8s-ops-agent
  namespace: default
  labels:
    app: k8s-ops-agent
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: k8s-ops-agent
